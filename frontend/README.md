# Smart City App - Frontend

```bash
flutter pub get
flutter run
```

**API base URL:** `lib/config/api_config.dart` — set `LOCAL_IP` for iOS device (e.g. `flutter run --dart-define=LOCAL_IP=192.168.X.X`).
