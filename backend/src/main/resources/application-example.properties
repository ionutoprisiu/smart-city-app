# Copy this file to application-local.properties and set your database credentials.
# application-local.properties is gitignored and will not be committed.
#
# cp application-example.properties application-local.properties

spring.datasource.username=your_db_username
spring.datasource.password=your_db_password
